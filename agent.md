# Agent Instructions

This project uses a **text-only** LLM. The model has **no multimodal capabilities**.

During development and optimization, do **not** invoke any multimodal features (image generation, vision, etc.). All inputs and outputs are text.
